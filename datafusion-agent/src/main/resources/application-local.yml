spring:
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
        register-enabled: false

datafusion:
  agent:
    manager:
      base-url: ${DATAFUSION_MANAGER_URL:http://127.0.0.1:8080}
