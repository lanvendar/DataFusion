spring:
  application:
    name: datafusion-agent
  profiles:
    default: local
  cloud:
    nacos:
      config:
        enabled: false
        import-check:
          enabled: false
      discovery:
        enabled: false
        register-enabled: false
