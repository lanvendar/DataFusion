# {feature} 数据结构定义

> 本文档是字段、类型、校验和层间映射的唯一事实源。实现不得自行增减字段或更改类型。

## 1. 表结构

### 1.1 表信息

- 表名: `{table_name}`
- 操作: 新建 / 修改 / 不涉及
- 主键:
- 说明:

### 1.2 DDL

```sql
-- CREATE TABLE 或 ALTER TABLE；不涉及数据库变更时写“无”
```

### 1.3 字段定义

| DB 列 | Java 字段 | Java 类型 | 必填 | 默认值 | 说明 |
|-------|-----------|-----------|------|--------|------|

> 审计字段如由 `BaseEntity` 或 `BaseIdEntity` 继承，Entity 不重复声明，但 DDL 中必须明确。

## 2. Entity / PO 映射

| Java 字段 | DB 列 | Java 类型 | 注解/处理器 | 说明 |
|-----------|-------|-----------|-------------|------|

> 不涉及 Entity / PO 时写“无”。

## 3. DTO 定义（可选）

> 仅定义本功能实际新增或修改的 DTO。没有新增或修改 DTO 时写“无”，不要为了模板完整性强行创建 DTO。

| DTO | 类型 | 使用场景 | 字段 | 字段类型 | 校验/查询方式 | 说明 |
|-----|------|----------|------|----------|---------------|------|

类型取值：

- `Request`: 请求入参
- `Response`: 响应 data
- `Query`: 查询条件
- `Internal`: Service 内部模型

## 4. API 数据映射

| API | 请求对象 | 响应 data | 响应包装 | 说明 |
|-----|----------|-----------|----------|------|

> 无 API 变更时写“无”。

## 5. 层间转换规则

| 方向 | 转换规则 | 特殊处理 |
|------|----------|----------|

示例方向：

- Request DTO -> Entity
- Update DTO -> Existing Entity
- Entity -> Response DTO
- External response -> Internal model

## 6. 枚举 / JSON / 特殊字段

| 字段 | 存储类型 | Java 类型 | 转换规则 | 说明 |
|------|----------|-----------|----------|------|

> 无枚举、JSON 或特殊字段时写“无”。

## 7. 复用对象

| 对象 | 路径 | 复用方式 | 说明 |
|------|------|----------|------|
